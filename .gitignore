# dependencies (bun install)
node_modules


# output
out
dist
*.tgz

mongo.txt
Standardized Meal Plan.Fitflix^L.xlsx
fitflix-storage-user_accessKeys.csv
mongo.txt

pnpm-lock.yaml

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store
.vercel
.env*

scratch/inspect-db.ts

archive_docs/

scratch/test-jwt-and-api.ts

# Vercel is no longer used — the backend is deployed to EC2 (api.fitflix.in).
# Ignored so a stray `vercel` CLI run cannot re-introduce the config or the
# generated bundle, which is what took production down on 2026-08-17.
vercel.json
api/index.bundle.cjs
